import React from 'react';
import Clock from './clock';

const Root = (props) => (
  <section>
    <Clock />
    // this is going to into Clock and gettn whatever was rendered
  </section>
);

export default Root;
